import {QuestionModalForm} from './QuestionModalForm'

const QuestionModalFormWrapper = (props: {question: any}) => {
  return <QuestionModalForm item={props.question} />
}

export {QuestionModalFormWrapper}
